-- 코드를 입력하세요
SELECT FLAVOR FROM (SELECT A.FLAVOR, SUM(A.TOTAL_ORDER + B.TOTAL_ORDER) FROM FIRST_HALF A, JULY B WHERE A.FLAVOR = B.FLAVOR GROUP BY A.FLAVOR ORDER BY SUM(A.TOTAL_ORDER + B.TOTAL_ORDER) DESC) WHERE ROWNUM < 4;